services:

  backend:
    build: ./backend
    container_name: resume-backend
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    container_name: resume-frontend
    ports:
      - "5173:80"

    depends_on:
      - backend